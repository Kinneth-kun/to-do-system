<x-layouts.app :title="'Updates: '.$task->title"><x-page-header title="Update history" :description="$task->title" :back="route('tasks.show', $task)" /><div class="card divide-y divide-slate-100">@forelse($updates as $update)<article class="card-body flex gap-3"><x-avatar :user="$update->user" size="sm" /><div class="min-w-0 flex-1"><div class="flex flex-wrap justify-between gap-2"><p class="text-sm font-medium text-slate-900">{{ $update->actorName() }}</p><time class="text-xs text-slate-400">{{ $update->created_at?->diffForHumans() }}</time></div><p class="mt-1 text-sm text-slate-600">{{ $update->type->label() }}</p>@if($update->remark)<p class="mt-2 whitespace-pre-line text-sm text-slate-500">{{ $update->remark }}</p>@endif</div></article>@empty<div class="card-body">No updates yet.</div>@endforelse</div><div class="mt-6">{{ $updates->links() }}</div></x-layouts.app>
